{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/build/detector-gpio",
            "args": ["--config=config/my-detector.conf"],
            "cwd": "${workspaceFolder}"
        }
    ]
}
